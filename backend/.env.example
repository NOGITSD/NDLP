# Core
GROQ_API_KEY=your-groq-api-key
APP_ENV=production
LOG_LEVEL=INFO

# CORS (comma-separated)
FRONTEND_ORIGIN=http://localhost,http://localhost:80

# Database backend: sqlite | firebase
DB_BACKEND=firebase
DB_PATH=data/jarvis.db

# Firebase credentials (inside container path)
# This should match the mounted file path in docker-compose.yml
FIREBASE_CREDENTIALS=ndlp-ea287-firebase-adminsdk-fbsvc-0a7288a18e.json

# Optional model overrides
GROQ_ANALYZER_MODEL=llama-3.1-8b-instant
GROQ_CHAT_MODEL=llama-3.3-70b-versatile

# Google OAuth (optional)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
