# ── Docker Compose Environment ──
# Copy this to .env and fill in values

# Google OAuth Client ID (passed to frontend at build time)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
